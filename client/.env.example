# Copy to .env.local. Vite dev server also proxies /api and /uploads to the
# server (see vite.config.ts), so this is mainly needed for production builds
# where client and server aren't served from the same origin.
VITE_API_URL="http://localhost:4000"
