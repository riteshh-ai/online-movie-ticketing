import { PageStub } from "../components/PageStub";

export function HomePage() {
  return <PageStub title="Home" note="Replaces legacy/index.php — slider, search, now-showing + coming-soon grids." />;
}
